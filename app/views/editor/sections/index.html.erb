<p id="notice"><%= notice %></p>

<h1>Secciones</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sections.each do |section| %>
      <tr>
        <td><%= section.name %></td>
        <td><%= link_to 'Ver', editor_section_path(section) %></td>
        <td><%= link_to 'Editar', edit_editor_section_path(section) %></td>
        <td><%= link_to 'Eliminar', editor_section_path(section), method: :delete, data: { confirm: 'Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Sección', new_editor_section_path %>
