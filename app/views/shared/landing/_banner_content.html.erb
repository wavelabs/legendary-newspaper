<div class="content">
  <div class="banner-content">
        <% if @top_banner && @top_banner.image.picture.exists? %>
		      <%=link_to(image_tag(@top_banner.image.picture, class: "img-responsive top-banner"),
		       @top_banner.link, target: :_blank)%>
        <% end %>
  </div>
</div>
