<div class="col-lg-3 col-md-3 col-sm-3 col-xs-12 ">
  <%= render 'shared/landing/weather' %>
  <div class="banner-right">
    <% if @banner_right_first && @banner_right_first.image.picture.exists? %>
      <%=link_to(image_tag(@banner_right_first.image.picture, class: "img-responsive"),
       @banner_right_first.link, target: :_blank)%>
    <% end %>
  </div>
  <div class="banner-right">
    <% if @banner_right_second && @banner_right_second.image.picture.exists? %>
      <%=link_to(image_tag(@banner_right_second.image.picture, class: "img-responsive"),
       @banner_right_second.link, target: :_blank)%>

    <% end %>
  </div>
  <div class="banner-right">
    <% if @banner_right_third && @banner_right_third.image.picture.exists? %>
      <%=link_to(image_tag(@banner_right_third.image.picture, class: "img-responsive"),
       @banner_right_third.link, target: :_blank)%>
    <% end %>
  </div>
</div>
