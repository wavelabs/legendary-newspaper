<p id="notice"><%= notice %></p>

<h1>Editors</h1>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @editors.each do |editor| %>
      <tr>
        <td><%= editor.email %></td>
        <td><%= link_to 'Show', admin_editor_path(editor) %></td>
        <td><%= link_to 'Edit', edit_admin_editor_path(editor) %></td>
        <td><%= link_to 'Destroy', admin_editor_path(editor), method: :delete, data: { confirm: 'Está Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Editor', new_admin_editor_path %>
